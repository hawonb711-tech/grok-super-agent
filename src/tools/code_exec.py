"""Code execution tool - uses x.ai server-side code_execution when available."""

# This module provides a fallback for when server-side tools are not used.
# The main agent uses xai_sdk.tools.code_execution() for Grok-native execution.
